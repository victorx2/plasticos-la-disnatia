# API Python — desarrollo local (proxy Vite; debe coincidir con API_PORT en backend/.env)
VITE_DEV_API_URL=http://127.0.0.1:8001

# Build de producción (npm run build): URL pública del API
# Debe coincidir con el reverse proxy (/api → backend)
VITE_API_BASE_URL=https://app.tudominio.com/api
