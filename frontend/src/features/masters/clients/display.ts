export {
  displayValue,
  entityAvatarPalette,
  entityInitials,
  formatLocation,
  formatPhoneDisplay,
  hasRif,
  truncateText,
} from "@/shared/catalog/entityDisplay"

// Backwards-compatible aliases for client module
export { entityInitials as clientInitials, entityAvatarPalette as clientAvatarPalette } from "@/shared/catalog/entityDisplay"
